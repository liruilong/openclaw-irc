# ── IRC Server ──
IRC_PORT=6667
IRC_CHANNEL=#miku
IRC_OPENCLAW_NICK=miku

# ── WebSocket ──
WS_PORT=9891

# ── MCP ──
MCP_HTTP_PORT=9894

# ── TTS (local fallback when no speakers registered) ──
# VOLCANO_API_KEY=your-api-key
# VOLCANO_CLUSTER=volcano_icl
# VOLCANO_VOICE=S_M1MG1D2W1
